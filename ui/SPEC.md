# Moved

The spec now lives at [`../SPEC.md`](../SPEC.md), at the repo root.

It was never a `ui/` document: it specifies the `.r49` v4 manifest (`lib/r49`), the occupancy
output contract, training-data derivation (`dataset/`, `classifier/`), the parameters in
`config.yaml`, and the project's licence — as well as the editor and live view.
