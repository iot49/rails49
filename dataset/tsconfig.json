{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    // The lib/* packages are consumed as TypeScript source and import each
    // other with explicit .ts extensions, so anything reaching into them needs
    // this. Every other consumer sets it too.
    "allowImportingTsExtensions": true
  },
  "include": ["src/**/*", "tests/**/*"]
}
